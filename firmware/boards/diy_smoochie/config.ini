[env:diy_smoochie]
board = diy_smoochie
board_build.partitions = firmware/partitions_16mb.csv
board_build.arduino.memory_type = qio_qspi
build_src_filter =${env.build_src_filter} +<../boards/diy_smoochie>
build_flags =
	${env.build_flags}
	-Ifirmware/boards/diy_smoochie

lib_deps =
	${env.lib_deps}
