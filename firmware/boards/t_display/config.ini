[env:t_display]
board = t_display
board_build.partitions = firmware/partitions_16mb.csv

build_src_filter = ${env.build_src_filter} +<../boards/t_display>
build_flags =
	${env.build_flags}
	-Ifirmware/boards/t_display

lib_deps =
	${env.lib_deps}
